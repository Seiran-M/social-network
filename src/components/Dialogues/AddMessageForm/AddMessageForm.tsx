import React, {FC} from 'react'import {Field, InjectedFormProps, reduxForm} from 'redux-form'import {maxLengthCreator, required} from '../../../utils/validator'import {Textarea} from '../../../common/FormsContfolls/FormsControlls'const maxLength50 = maxLengthCreator(50)const AddMessageForm: FC<InjectedFormProps<FormDataType>> = React.memo((props) => {   return (      <form onSubmit={props.handleSubmit}>         <div>            <Field component={Textarea}                   validate={[required, maxLength50]}                   name={'newMessageBody'}                   placeholder={'Enter your message'}/>            <button>Send</button>         </div>      </form>   )})export const AddMessageFormRedux = reduxForm<FormDataType>({form: 'DialoguesAddMessageForm'})(AddMessageForm)// typesexport type FormDataType = {   newMessageBody: string}