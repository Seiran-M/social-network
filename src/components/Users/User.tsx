import React, {FC} from 'react'import {NavLink} from 'react-router-dom'import {Usertype} from '../../redux/users-reducer'import styles from './Users.module.css'import userPhoto from '../../assets/images/user.png'export type PropsType = {   user: Usertype   follow: (userID: number) => void   unfollow: (userID: number) => void   followingInProgress: any[]}export const User: FC<PropsType> = ({user, followingInProgress, follow, unfollow, ...props}) => {   return (      <>         <div>            <div>               <NavLink to={'/profile/' + user.id}>                  <img src={user.photos.small ? user.photos.small : userPhoto} className={styles.userPhoto}/>               </NavLink>            </div>            <div>               {                  user.followed                     ? <button disabled={followingInProgress                        .some(id => id === user.id)} onClick={() => {unfollow(user.id)}}>unfollow</button>                     : <button disabled={followingInProgress.some(id => id === user.id)} onClick={() => {follow(user.id)}}>follow</button>               }            </div>         </div>         <div>            <div>{user.name}</div>            <div>{user.status}</div>            <span>               <div>{'user.location.country'}</div>               <div>{'user.location.city'}</div>            </span>         </div>      </>   )}