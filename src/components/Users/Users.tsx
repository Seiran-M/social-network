import React, {FC} from 'react'import {Usertype} from '../../redux/users-reducer'import {Pagination} from '../../common/Pagination/Pagination'import {User} from './User'export const Users: FC<UsersPropsType> = React.memo((props) => {   const {users, pageSize, currentPage, totalUsersCount, onPageChanged, followingInProgress, unfollow, follow} = props   return (      <>         <Pagination onPageChanged={onPageChanged} pageSize={pageSize} totalUsersCount={totalUsersCount}                     currentPage={currentPage}/>         <div>            {               users.map(user => <User key={user.id}                                       followingInProgress={followingInProgress}                                       follow={follow}                                       unfollow={unfollow}                                       user={user}               />)            }         </div>      </>   )})// typestype UsersPropsType = {   users: Array<Usertype>   follow: (userId: number) => void   unfollow: (userId: number) => void   onPageChanged: (p: number) => void   pageSize: number   totalUsersCount: number   currentPage: number   followingInProgress: any[]}