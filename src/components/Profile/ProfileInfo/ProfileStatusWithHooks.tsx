import React, {ChangeEvent, FC, useEffect, useState} from 'react'export const ProfileStatusWithHooks: FC<PropsType> = React.memo( (props) => {   const [editMode, setEditMode] = useState(false)   const [status, setStatus] = useState(props.status)   useEffect(() => setStatus(props.status), [props.status])   const onEditMode = () => {      setEditMode(true)   }   const offEditMode = () => {      props.updateUserStatus(status)      setEditMode(false)   }   const onStatusChange = (e: ChangeEvent<HTMLInputElement>) => {      setStatus(e.currentTarget.value)   }   return (      <>         {!editMode            ? <div>               <span onDoubleClick={onEditMode}> Status: {props.status || 'enter your status'}</span>            </div>            : <div>               <input onChange={onStatusChange} autoFocus={true} onBlur={offEditMode} value={status}/>            </div>         }      </>   )})// typestype PropsType = {   status: string   updateUserStatus: (status: string) => void}