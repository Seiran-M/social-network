import {addPost, deletePost, profileReducer} from './profile-reducer'const state = {   posts: [      {id: 0, message: 'Hi, how are you?', likesCount: 12},      {id: 1, message: 'It\'s my first post!', likesCount: 55},      {id: 2, message: 'Bye', likesCount: 0},      {id: 3, message: 'Good', likesCount: 30},   ],   profile: null,   status: '',   newPostText: '',}test('length of post should be incremented', () => {   // 1. Test Data   const action = addPost('Test post')   // 2. Action   const newState = profileReducer(state, action)   // 3. Expected result   expect(newState.posts.length).toBe(5)})test('added post should be correct', () => {   // 1. Test Data   const action = addPost('Test post')   // 2. Action   const newState = profileReducer(state, action)   // 3. Expected result   expect(newState.posts[4].message).toBe('Test post')})test('length of messages should be decrement', () => {   // 1. Test Data   const action = deletePost(1)   // 2. Action   const newState = profileReducer(state, action)   // 3. Expected result   expect(newState.posts.length).toBe(3)})test("if postId is incorrect, length of messages shouldn't be decrement", () => {   // 1. Test Data   const action = deletePost(165)   // 2. Action   const newState = profileReducer(state, action)   // 3. Expected result   expect(newState.posts.length).toBe(4)})